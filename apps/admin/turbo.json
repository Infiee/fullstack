{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "dependsOn": ["^pack"],
      "cache": false,
      "inputs": [
        "src",
        "node_modules/@repo",
        "../../packages",
        "packages"
      ]
    }
  }
}
